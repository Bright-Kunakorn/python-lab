def isPrime(n, i = 2):
    #เช็คว่า n เป็น จำนวนเฉพาะไหม

def sum_prime_in_range(x,y):
    num = list(range(x, y+1, 1))  
    #gen ค่าตั้งแต่x-y
    return sum(num) #เรียกใช้functionผลรวมของจำนวนเฉพาะ


def sum(num):
    #เรียกใช้function isPrime(n)ถ้าใช้ก็เอาไปบวก
    #return ผลรวมตัวเฉพาะ
    

print(sum_prime_in_range(2,20))